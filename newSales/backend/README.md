# SalesDream Backend

## Setup
1. Copy `.env.example` to `.env` and fill values.
2. Install dependencies
   ```bash
   npm install
   ```
3. Start
   ```bash
   npm run dev
   ```

## MySQL schema
See `schema.sql` for starter tables.
